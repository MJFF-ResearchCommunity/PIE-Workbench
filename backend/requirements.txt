# PIE Workbench Backend Dependencies
#
# This file bundles everything the backend needs, including the deps required
# by the `lib/PIE` and `lib/PIE-clean` submodules. PIE's setup.py does not
# declare install_requires, so listing these here lets users do a single
# `pip install -r requirements.txt` instead of chasing multiple files.

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# Core data
pandas>=2.1.0
numpy>=1.24.0
scipy>=1.11.0
pyarrow>=14.0.0

# Machine learning
scikit-learn>=1.3.0
endgame-ml[tabular]
shap>=0.41.0

# Classical statistics
statsmodels>=0.14.0
pingouin>=0.5.0
scikit-posthocs>=0.9.0
lifelines>=0.27.0

# Visualization (used by PIE notebooks / figure generation)
matplotlib>=3.4.3
seaborn>=0.11.2
plotly>=5.3.1

# System utilities
psutil>=5.9.0
